prompt_order = [
    "directory",
    "git_branch",
    "git_commit",
    "git_state",
    "git_status",
    # "golang",
    # "rust",
    # "nix-shell",
    "env_var",
    "cmd_duration",
    "line_break",
    "jobs",
    "character"
]

[directory]
truncation_length = 5

[git_status]
symbol = " "

[golang]
symbol = " "

[env_var]
variable = "KAKOUNE_SESSION"
#TODO: replace this with a format string when 0.45.0 comes out, to remove the with
prefix = " "
style = "bold cyan"
